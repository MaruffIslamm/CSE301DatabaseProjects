CREATE TABLE  "INSTRUCTOR" 
   (	"ID" VARCHAR2(5), 
	"NAME" VARCHAR2(20), 
	"DEPT_NAME" VARCHAR2(20), 
	"SALARY" NUMBER(8,2), 
	 CONSTRAINT "SALARY_INS_NN" CHECK (SALARY IS NOT NULL) ENABLE, 
	 CONSTRAINT "SALARY_INS_CHK" CHECK (SALARY >= 0) ENABLE, 
	 CONSTRAINT "ID_INS_PK" PRIMARY KEY ("ID") ENABLE, 
	 CONSTRAINT "INSTRUCTOR_DEPT_NAME_FK" FOREIGN KEY ("DEPT_NAME")
	  REFERENCES  "DEPARTMENT" ("DEPT_NAME") ON DELETE SET NULL ENABLE
   )
/
CREATE TABLE  "STUDENT" 
   (	"ID" VARCHAR2(5), 
	"NAME" VARCHAR2(20), 
	"DEPT_NAME" VARCHAR2(20), 
	"TOT_CRED" NUMBER(3,0), 
	 CONSTRAINT "ID_STUDENT_NN" CHECK (ID IS NOT NULL) ENABLE, 
	 CONSTRAINT "NAME_STUDENT_NN" CHECK (NAME IS NOT NULL) ENABLE, 
	 CONSTRAINT "TOT_CRED_CHK" CHECK (TOT_CRED >=0) ENABLE, 
	 CONSTRAINT "ID_STUDENT_PK" PRIMARY KEY ("ID") ENABLE, 
	 CONSTRAINT "DEPT_NAME_STU_FK" FOREIGN KEY ("DEPT_NAME")
	  REFERENCES  "DEPARTMENT" ("DEPT_NAME") ON DELETE CASCADE ENABLE
   )
/
CREATE TABLE  "ADVISOR" 
   (	"S_ID" VARCHAR2(5), 
	"I_ID" VARCHAR2(5), 
	 CONSTRAINT "S_ID_ADVISOR_PK" PRIMARY KEY ("S_ID") ENABLE, 
	 CONSTRAINT "S_ID_ADVISOR_FK" FOREIGN KEY ("S_ID")
	  REFERENCES  "STUDENT" ("ID") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "I_ID_ADVISOR_FK" FOREIGN KEY ("I_ID")
	  REFERENCES  "INSTRUCTOR" ("ID") ON DELETE SET NULL ENABLE
   )
/
CREATE TABLE  "CLASSROOM" 
   (	"BUILDING" VARCHAR2(15), 
	"ROOM_NO" CHAR(7), 
	"CAPACITY" NUMBER(4,0), 
	 CONSTRAINT "CLASSROOM_BUILDING_NN" CHECK (building is not null) ENABLE, 
	 CONSTRAINT "ROOM_NO_NN" CHECK (room_no is not null) ENABLE, 
	 CONSTRAINT "CAPACITY_NN" CHECK (capacity is not null) ENABLE, 
	 CONSTRAINT "CLASSROOM_BUILDING_ROOM_PK" PRIMARY KEY ("BUILDING", "ROOM_NO") ENABLE
   )
/
CREATE TABLE  "DEPARTMENT" 
   (	"DEPT_NAME" VARCHAR2(20), 
	"BUILDING" VARCHAR2(15), 
	"BUDGET" NUMBER(12,2), 
	 CONSTRAINT "BUILDING_NN" CHECK (BUILDING IS NOT NULL) ENABLE, 
	 CONSTRAINT "BUDGET_NN" CHECK (BUDGET IS NOT NULL) ENABLE, 
	 CONSTRAINT "DEPT_BUDGET_CHK" CHECK (BUDGET >= 0) ENABLE, 
	 CONSTRAINT "DEPT_NAME_PK" PRIMARY KEY ("DEPT_NAME") ENABLE
   )
/
CREATE TABLE  "COURSE" 
   (	"COURSE_ID" VARCHAR2(8), 
	"TITLE" VARCHAR2(50), 
	"DEPT_NAME" VARCHAR2(20), 
	"CREDITS" NUMBER(2,0), 
	 CONSTRAINT "TITLE_COURSE_NN" CHECK (TITLE IS NOT NULL) ENABLE, 
	 CONSTRAINT "CREDITS_COURSE_CHK" CHECK (CREDITS >= 0) ENABLE, 
	 CONSTRAINT "COURSE_ID_PK" PRIMARY KEY ("COURSE_ID") ENABLE, 
	 CONSTRAINT "DEPT_NAME_COURSE_FK" FOREIGN KEY ("DEPT_NAME")
	  REFERENCES  "DEPARTMENT" ("DEPT_NAME") ON DELETE SET NULL ENABLE
   )
/
CREATE TABLE  "PREREQ" 
   (	"COURSE_ID" VARCHAR2(8), 
	"PREREQ_ID" VARCHAR2(8), 
	 CONSTRAINT "PREREQ_COURSE_PRE_PK" PRIMARY KEY ("COURSE_ID", "PREREQ_ID") ENABLE, 
	 CONSTRAINT "PRE_COURSE_ID_FK" FOREIGN KEY ("COURSE_ID")
	  REFERENCES  "COURSE" ("COURSE_ID") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "PRE_PREREQ_ID_FK" FOREIGN KEY ("PREREQ_ID")
	  REFERENCES  "COURSE" ("COURSE_ID") ON DELETE CASCADE ENABLE
   )
/
CREATE TABLE  "SECTION" 
   (	"COURSE_ID" VARCHAR2(8), 
	"SEC_ID" VARCHAR2(8), 
	"SEMESTER" VARCHAR2(6), 
	"YEAR" NUMBER(4,0), 
	"ROOM_NO" CHAR(7), 
	"BUILDING" VARCHAR2(15), 
	"TIME_SLOT_ID" VARCHAR2(4), 
	 CONSTRAINT "SECTION_SEMESTER_CHK" CHECK (SEMESTER IN ('Spring','Winter','Fall','Summer')) ENABLE, 
	 CONSTRAINT "SECTION_PK" PRIMARY KEY ("COURSE_ID", "SEC_ID", "SEMESTER", "YEAR") ENABLE, 
	 CONSTRAINT "SECTION_CI_FK" FOREIGN KEY ("COURSE_ID")
	  REFERENCES  "COURSE" ("COURSE_ID") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "SECTION_B_R_FK" FOREIGN KEY ("BUILDING", "ROOM_NO")
	  REFERENCES  "CLASSROOM" ("BUILDING", "ROOM_NO") ON DELETE SET NULL ENABLE
   )
/
CREATE TABLE  "TAKES" 
   (	"ID" VARCHAR2(5), 
	"COURSE_ID" VARCHAR2(8), 
	"SEC_ID" VARCHAR2(8), 
	"SEMESTER" VARCHAR2(6), 
	"YEAR" NUMBER(4,0), 
	"GRADE" VARCHAR2(2), 
	 CONSTRAINT "TAKES_ID_CI_SI_SEM_YR_PK" PRIMARY KEY ("ID", "COURSE_ID", "SEC_ID", "SEMESTER", "YEAR") ENABLE, 
	 CONSTRAINT "TAKES_ID_FK" FOREIGN KEY ("ID")
	  REFERENCES  "STUDENT" ("ID") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "TAKES_CI_SI_SEM_YR_FK" FOREIGN KEY ("COURSE_ID", "SEC_ID", "SEMESTER", "YEAR")
	  REFERENCES  "SECTION" ("COURSE_ID", "SEC_ID", "SEMESTER", "YEAR") ON DELETE CASCADE ENABLE
   )
/
CREATE TABLE  "TEACHES" 
   (	"ID" VARCHAR2(10), 
	"COURSE_ID" VARCHAR2(10), 
	"SEC_ID" VARCHAR2(8), 
	"SEMESTER" VARCHAR2(6), 
	"YEAR" NUMBER(4,0), 
	 CONSTRAINT "TEACHES_ID_CI_SI_SEM_YR_PK" PRIMARY KEY ("ID", "COURSE_ID", "SEC_ID", "SEMESTER", "YEAR") ENABLE, 
	 CONSTRAINT "TEACHES_CI_SI_SEM_YR_FK" FOREIGN KEY ("COURSE_ID", "SEC_ID", "SEMESTER", "YEAR")
	  REFERENCES  "SECTION" ("COURSE_ID", "SEC_ID", "SEMESTER", "YEAR") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "TEACHES_ID_FK" FOREIGN KEY ("ID")
	  REFERENCES  "INSTRUCTOR" ("ID") ON DELETE CASCADE ENABLE
   )
/
CREATE TABLE  "TIME_SLOT" 
   (	"TIME_SLOT_ID" VARCHAR2(4), 
	"DAY" VARCHAR2(1), 
	"START_HOUR" NUMBER(2,0), 
	"START_MIN" NUMBER(2,0), 
	"END_HOUR" NUMBER(2,0), 
	"END_MIN" NUMBER(2,0), 
	 CONSTRAINT "START_HOUR_CHK" CHECK (START_HOUR>=0 AND START_HOUR<=24) ENABLE, 
	 CONSTRAINT "START_MIN_CHK" CHECK (START_MIN >=0 AND START_MIN <=60) ENABLE, 
	 CONSTRAINT "END_HOUR_CHK" CHECK (END_HOUR >=0 AND END_HOUR <=24) ENABLE, 
	 CONSTRAINT "END_MIN_CHK" CHECK (END_MIN >=0 AND END_MIN <=60) ENABLE, 
	 CONSTRAINT "TIME_SLOT_PK" PRIMARY KEY ("TIME_SLOT_ID", "DAY", "START_HOUR", "START_MIN") ENABLE
   )
/

Rem No function found to generate DDL.

CREATE UNIQUE INDEX  "CLASSROOM_BUILDING_ROOM_PK" ON  "CLASSROOM" ("BUILDING", "ROOM_NO")
/
CREATE UNIQUE INDEX  "COURSE_ID_PK" ON  "COURSE" ("COURSE_ID")
/
CREATE UNIQUE INDEX  "DEPT_NAME_PK" ON  "DEPARTMENT" ("DEPT_NAME")
/
CREATE UNIQUE INDEX  "ID_INS_PK" ON  "INSTRUCTOR" ("ID")
/
CREATE UNIQUE INDEX  "ID_STUDENT_PK" ON  "STUDENT" ("ID")
/
CREATE UNIQUE INDEX  "PREREQ_COURSE_PRE_PK" ON  "PREREQ" ("COURSE_ID", "PREREQ_ID")
/
CREATE UNIQUE INDEX  "SECTION_PK" ON  "SECTION" ("COURSE_ID", "SEC_ID", "SEMESTER", "YEAR")
/
CREATE UNIQUE INDEX  "S_ID_ADVISOR_PK" ON  "ADVISOR" ("S_ID")
/
CREATE UNIQUE INDEX  "TAKES_ID_CI_SI_SEM_YR_PK" ON  "TAKES" ("ID", "COURSE_ID", "SEC_ID", "SEMESTER", "YEAR")
/
CREATE UNIQUE INDEX  "TEACHES_ID_CI_SI_SEM_YR_PK" ON  "TEACHES" ("ID", "COURSE_ID", "SEC_ID", "SEMESTER", "YEAR")
/
CREATE UNIQUE INDEX  "TIME_SLOT_PK" ON  "TIME_SLOT" ("TIME_SLOT_ID", "DAY", "START_HOUR", "START_MIN")
/

Rem No package found to generate DDL.

Rem No procedure found to generate DDL.

Rem No sequence found to generate DDL.

Rem No synonym found to generate DDL.

Rem No trigger found to generate DDL.

Rem No view found to generate DDL.