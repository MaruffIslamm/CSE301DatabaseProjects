CREATE TABLE  "EMPLOYEE" 
   (	"EMP_ID" VARCHAR2(10), 
	"EMP_NAME" VARCHAR2(20), 
	"DIVISION" VARCHAR2(15), 
	"DOB" DATE, 
	 CONSTRAINT "EMPLOYEE_EMP_ID_PK" PRIMARY KEY ("EMP_ID") ENABLE, 
	 CONSTRAINT "EMPLOYEE_DIV_CHK" CHECK (division in ('Dhaka','Rajshahi','Khulna','Barishal','Sylhet','Chittagong','Rangpur')) ENABLE, 
	 CONSTRAINT "EMPLOYEE_EMP_ID_CHK" CHECK (emp_id like 'E%') ENABLE
   )
/
CREATE TABLE  "CHILDREN" 
   (	"EMP_ID" VARCHAR2(10), 
	"CHILD_SEQ" VARCHAR2(10), 
	"NAME" VARCHAR2(20), 
	 CONSTRAINT "CHILDREN_EMP_ID_CHILD_SEQ_PK" PRIMARY KEY ("EMP_ID", "CHILD_SEQ") ENABLE, 
	 CONSTRAINT "CHILDREN_EMP_ID_FK" FOREIGN KEY ("EMP_ID")
	  REFERENCES  "EMPLOYEE" ("EMP_ID") ON DELETE CASCADE ENABLE
   )
/
CREATE TABLE  "COMPANY" 
   (	"COM_ID" VARCHAR2(10), 
	"COM_NAME" VARCHAR2(20), 
	"LOCATION" VARCHAR2(20), 
	 CONSTRAINT "COMPANY_COM_ID_PK" PRIMARY KEY ("COM_ID") ENABLE
   )
/
CREATE TABLE  "JOB" 
   (	"EMP_ID" VARCHAR2(10), 
	"COM_ID" VARCHAR2(10), 
	"DESIGNATION" VARCHAR2(20), 
	"SALARY" NUMBER(12,2), 
	 CONSTRAINT "JOB_EMP_ID_PK" PRIMARY KEY ("EMP_ID") ENABLE, 
	 CONSTRAINT "JOB_SALARY_CHK" CHECK (salary > 10000) ENABLE, 
	 CONSTRAINT "JOB_EMP_ID_FK" FOREIGN KEY ("EMP_ID")
	  REFERENCES  "EMPLOYEE" ("EMP_ID") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "JOB_COM_ID_FK" FOREIGN KEY ("COM_ID")
	  REFERENCES  "COMPANY" ("COM_ID") ON DELETE CASCADE ENABLE
   )
/
CREATE TABLE  "MANAGES" 
   (	"EMP_ID" VARCHAR2(10), 
	"MNG_ID" VARCHAR2(10), 
	 CONSTRAINT "MANAGES_EMP_ID_PK" PRIMARY KEY ("EMP_ID") ENABLE, 
	 CONSTRAINT "MANAGES_EMP_ID_FK" FOREIGN KEY ("EMP_ID")
	  REFERENCES  "EMPLOYEE" ("EMP_ID") ON DELETE CASCADE ENABLE, 
	 CONSTRAINT "MANAGES_MNG_ID_FK" FOREIGN KEY ("MNG_ID")
	  REFERENCES  "EMPLOYEE" ("EMP_ID") ON DELETE CASCADE ENABLE
   )
/
Rem No function found to generate DDL.
CREATE UNIQUE INDEX  "CHILDREN_EMP_ID_CHILD_SEQ_PK" ON  "CHILDREN" ("EMP_ID", "CHILD_SEQ")
/
CREATE UNIQUE INDEX  "COMPANY_COM_ID_PK" ON  "COMPANY" ("COM_ID")
/
CREATE UNIQUE INDEX  "EMPLOYEE_EMP_ID_PK" ON  "EMPLOYEE" ("EMP_ID")
/
CREATE UNIQUE INDEX  "JOB_EMP_ID_PK" ON  "JOB" ("EMP_ID")
/
CREATE UNIQUE INDEX  "MANAGES_EMP_ID_PK" ON  "MANAGES" ("EMP_ID")
/
Rem No package found to generate DDL.
Rem No procedure found to generate DDL.
Rem No sequence found to generate DDL.
Rem No synonym found to generate DDL.
Rem No trigger found to generate DDL.
Rem No view found to generate DDL.
